export const method = { id: "remotion", guide: "Use Remotion 4, @remotion/bundler, @remotion/renderer, React, local Rspack, Playwright Chromium, and FFmpeg. Write one self-contained .mjs/.js script with registerRoot() and Composition; bundle with {rspack: true}; use renderStill() for image or renderMedia() for video; inspect once, repair only if invalid. Use local Chromium when available; no package search/install, npx, or network.", tools: ["Remotion 4", "@remotion/bundler", "@remotion/renderer", "React", "Rspack", "Playwright Chromium", "FFmpeg"] };
